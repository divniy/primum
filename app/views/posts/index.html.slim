- content_for :javascripts do
  - if Rails.env == 'production'
    = javascript_include_tag 'live'

.row
  .span3
    /= render 'tag_filter'
    /= render 'tag_categories/tags_filter'
    = render 'filters/filters_bar'
  .span9
    .new_post
      = render 'form'
    .headline
      = render @posts